#compdef open-proj

_arguments -C \
	'*:: :->args' \
	&& ret=0

case "$state" in
	args)
		generators=()
		ls "$PROJECTS/" | while read file
		do
			generators+=("$(basename "$file")")
		done

		_describe -t generators 'generator' generators && ret=0
		;;
esac

return 1
